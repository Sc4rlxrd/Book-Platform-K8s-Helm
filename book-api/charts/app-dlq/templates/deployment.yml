apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ include "app-dlq.fullname" . }}
  labels:
    {{- include "app-dlq.labels" . | nindent 4 }}
spec:
  replicas: {{ .Values.replicaCount }}
  selector:
    matchLabels:
      {{- include "app-dlq.selectorLabels" . | nindent 6 }}
  template:
    metadata:
      labels:
        {{- include "app-dlq.selectorLabels" . | nindent 8 }}
    spec:
      containers:
        - name: {{ .Chart.Name }}
          image: "{{ .Values.image.repository }}:{{ .Values.image.tag | default .Chart.AppVersion }}"
          imagePullPolicy: {{ .Values.image.pullPolicy }}
          ports:
            - containerPort: {{ .Values.service.port }}
          resources:
            {{- toYaml .Values.resources | nindent 12 }}
          envFrom:
            - configMapRef:
                name: {{ include "app-dlq.fullname" . }}-config
            - secretRef:
                name: {{ include "app-dlq.fullname" . }}-credentials